# ابدأ من هنا

## وش داخل هذا المجلد

| المجلد / الملف | وش فيه |
|---|---|
| **`TeamUp-الموقع.html`** | **الموقع كامل شغّال — افتحه بنقرة مزدوجة** |
| `src/` | الكود المصدري — هنا الشغل |
| `supabase/` | ملفات SQL الخمسة + Edge Function |
| `ملفات-جاهزة/` | التصميم الأصلي + دليل ربط Supabase |
| `.vscode/` | إعدادات المحرر (مضبوطة للمشروع) |
| `README.md` | **الشرح الكامل** — اقرأه قبل ما تبدأ |
| `CLAUDE.md` | ذاكرة المشروع: القرارات والأخطاء المكلفة اللي انحلّت — اقرأه قبل أي تعديل كبير |
| `.env` | مفاتيح Supabase (جاهزة) |

---

## أسرع طريقة تشوف الموقع شغّال

افتح **`TeamUp-الموقع.html`** بنقرة مزدوجة — يشتغل مباشرة بدون تثبيت ولا خادم،
ومربوط بقاعدة البيانات الحقيقية. سجّل حساب وجرّب كل شي.

---

## عشان تطوّر فيه

افتح هذا المجلد في VS Code (**File ← Open Folder**)، ثم في التيرمنال:

```bash
npm install
```

```bash
npm run dev
```

يعطيك رابط `http://localhost:5173`.

> إذا طلع خطأ في `vite` بعد التثبيت:
> ```bash
> npm approve-scripts esbuild
> npm rebuild esbuild
> ```

---

## البناء

```bash
npm run build:single
```

يطلع ملف HTML واحد في `dist-single/` يشتغل بالنقر المزدوج بدون خادم.

---

## قبل ما تعدّل أي شي

اقرأ قسم **«أعراف لازم نلتزم بها»** في `README.md`. فيه أربع قواعد تختصر عليك
وقتًا كثيرًا، أهمها:

1. المنطق كله في `App.jsx` داخل كائن `V` — ملفات `generated/` تعرض فقط
2. لا تغيّر التصميم، ولا تُدخِل مكتبة CSS
3. أي جدول جديد يحتاج `GRANT` مو بس سياسة RLS
4. النصوص كلها في `src/lib/strings.js` عربي/إنجليزي

> **التنسيق التلقائي مطفأ عمدًا** في `.vscode/settings.json` — ملفات `generated/`
> فيها أنماط سطرية دقيقة وإعادة التنسيق تكسر شكل الموقع.

---

## ملفات SQL — بالترتيب

من Supabase ← SQL Editor ← New query:

`01_schema.sql` ← `02_skills.sql` ← `04_storage_and_avatars.sql` ← `05_notifications.sql`

(`03_demo_members_optional.sql` اختياري — بيانات تجريبية للتطوير فقط.)

---

## سير العمل

```bash
git checkout -b feature/اسم-الميزة
git commit -m "وصف التغيير"
git push -u origin feature/اسم-الميزة
```

ثم Pull Request إلى `main`. التفاصيل في `README.md`.
